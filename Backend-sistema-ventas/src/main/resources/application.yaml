spring:
  application:
    name: sistema-ventas
  datasource:
    url: jdbc:postgresql://localhost:5432/ventas_db
    username: postgres
    password: jose20
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  flyway:
    enabled: false

server:
  port: 8080

jwt:
  secret: MiClaveSecretaSuperSeguraParaJWTSistemaVentas2024ConAlMenos256Bits!!@$
  expiration: 900000
  refresh-expiration: 604800000

app:
  cors:
    allowed-origins: http://localhost:3000
