spring:
  application:
    name: sistema-ventas
  datasource:
    url: jdbc:postgresql://localhost:5432/sistema_ventas
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true

server:
  port: 8080

jwt:
  secret: MiClaveSecretaSuperSeguraParaJWTSistemaVentas2024ConAlMenos256Bits!!@$
  expiration: 900000
  refresh-expiration: 604800000

app:
  cors:
    allowed-origins: http://localhost:3000
